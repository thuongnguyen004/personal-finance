spring:
  application:
    name: persional-finance

  datasource:
    url: jdbc:postgresql://localhost:5432/database-name
    username: username
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true